name: MasterDeployCI
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [8.x, 10.x, 12.x]
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install Packages
        run: npm install
      - name: Deploy to GH Pages
        run: |
          git config --global user.email "daffaputradamar@gmail.com"
          git config --global user.name "daffaputradamar"
          git remote set-url origin https://ee3d8d6feeb7ca5ebc8f7307b227ab1ac58d9809@github.com/daffaputradamar/WebFramework.git
          npm run deploy
